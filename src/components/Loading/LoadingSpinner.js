import React from 'react';
import './loading-spinner.scss';

/**
 * LoadingSpinner
 *
 * Small indeterminate loading animation
 *
 * @return {ReactElement}
 *
 * @example
 * <LoadingSpinner />
 *
 */
const LoadingSpinner = () => (
  <div className="usaf-loading-spinner" />
);

export default LoadingSpinner;
